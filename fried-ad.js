document.querySelectorAll('.fried-banner').forEach(banner=>{const button=banner.querySelector('.fried-pause');button.addEventListener('click',()=>{const paused=banner.classList.toggle('is-paused');button.setAttribute('aria-pressed',String(paused));button.setAttribute('aria-label',paused?'Resume scrolling FRIED advertisement':'Pause scrolling FRIED advertisement');button.textContent=paused?'Resume':'Pause';});});
